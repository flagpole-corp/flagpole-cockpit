import { Box } from '@mui/material'
import { PricingOptions } from '~/components'

export const Pricing = (): JSX.Element => {
  return (
    <Box sx={{ width: '100%' }}>
      <PricingOptions />
    </Box>
  )
}
